-- ============================================================
--  Krent: убираем избыточную SELECT-политику листинга bucket.
--  Публичный bucket отдаёт файлы по прямому URL без политики;
--  широкая SELECT-политика лишь позволяла листинг всех файлов.
-- ============================================================

drop policy if exists "property_media_storage_read" on storage.objects;
